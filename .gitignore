# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Logs and temp
*.log
*.tmp

# Build output / packaged extensions
*.zip
*.crx
*.pem
dist/
build/

# Node (if you ever add tooling)
node_modules/
package-lock.json

# Native messaging host manifest — generated per-machine by install.bat
# (contains an absolute path), so it must not be committed.
native-host/com.moodle_hoarder.updater.json

# Local secrets
.env
.env.local
__pycache__/
*.py[cod]
# Python virtual environments
.venv/
venv/
transcriber/.venv/
